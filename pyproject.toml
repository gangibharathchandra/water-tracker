[project]
name = "water-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "pandas",
    "pre-commit>=4.6.0",
]

[tool.ruff]
line-length = 100

[tool.mypy]
ignore_missing_imports = true

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
